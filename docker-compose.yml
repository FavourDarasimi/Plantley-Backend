services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
    command: gunicorn core.wsgi:application --bind 0.0.0.0:8000 --workers 4

volumes:
  static_volume:
